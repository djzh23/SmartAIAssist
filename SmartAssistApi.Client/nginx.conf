server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Ensure WebAssembly MIME type is served correctly.
    types {
        application/wasm wasm;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
